In this post, i will try to let's try to explore the different fields in `package.json`. This tells pretty much about the
package we are building and the format of `js code` that we are exporting.

Here are few things we can know from `package.json`
